using UnityEngine;
using System;

public class Exercise01 : Monobehaviour

{
    string positiong = "3, 5, 6";
    //split()를 이용해 문자열을 분할하면 분할된 개수만큼 배열에 저장
    //쉼표를 기준으로 분할

    string[] str = position.Split(',');
    Debug.Log($"{str{0}, {str[1]}, {str[2]});

    string str2 = "Hello, World";
    // 7번째 위치부터 뒤에 있는 문자열만 다시 저장

    str2 = str2.Substring(7);
    Debug.Log(str2);


}
